# Simulation Sweet Python Package

This package is used to test and simulate interactions with the CAN-D data logger.

# Setup

* Install python3 (3.7 if you can)
* Install the dependancies: `pip install -r requirements`
* Install this package: `pip install -e .`

# Usage

Coming Soon to a readme near you.

